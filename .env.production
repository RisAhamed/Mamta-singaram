# Production environment - API calls go to same origin (no base URL needed)
# The backend must be accessible at the same domain as the frontend
# OR set VITE_API_URL to your production backend URL before building
VITE_API_URL=
